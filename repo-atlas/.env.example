# Copy to .env and fill in a real key ONLY for manual, billed runs.
# The env-var NAME is config-driven: it must match "api_key_env" in config/atlas.json.
# With no key set, the gatekeeper falls back to the offline mock client and the whole
# tool — tests, vault, brief, comparison — still runs.
ATLAS_API_KEY=
