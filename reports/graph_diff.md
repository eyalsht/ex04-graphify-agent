# PRE-FIX vs POST-FIX Graph Diff (R5.6.3)

> Structural evidence that the fix changed the *shape* of the code, not just the text.
> The machine-rendered section below is produced verbatim by
> `token_comparison.graph_diff.diff_graphs` + `render_graph_diff` over two committed
> artifacts — **no hand-typed numbers** (CLAUDE.md §4):
>
> - **PRE-FIX:** [`artifacts/graphify/graph.json`](../artifacts/graphify/graph.json) (immutable baseline)
> - **POST-FIX:** [`artifacts/graphify_post_fix/graph.json`](../artifacts/graphify_post_fix/graph.json)
>   — regenerated by `graphify update data/broken-python` (AST extraction, **0 tokens**,
>   keyless) after the fix.
>
> Reproduce: `uv run python -c "from ex04_graphify_agent.token_comparison.graph_diff import
> diff_graphs, render_graph_diff; print(render_graph_diff(diff_graphs('artifacts/graphify/graph.json',
> 'artifacts/graphify_post_fix/graph.json')))"`

## R5.6.3 - PRE-FIX vs POST-FIX graph diff

- Nodes: 23 -> 23
- Edges: 20 -> 17
- Communities (PRE): [0, 1, 2, 3, 4, 5]
- Communities (POST): [0, 1, 2, 3, 4, 5, 6]
- Removed nodes: license_mit_license, mathsquiz_mathsquiz_final_py, polygons_polygons_rationale_18, polygons_polygons_rationale_33, polygons_polygons_rationale_50
- Added nodes: mathsquiz_readme, mathsquiz_readme_introduction, mathsquiz_readme_objectives, mathsquiz_readme_the_files, readme
- Usage edge: polygons_polygons_calc_polygon_details -calls-> polygons_polygons_polygon: Polygon is used (not dead) post-fix.

## Interpretation

The diff has two distinct stories. Keep them separate.

### 1. Code-graph changes — caused by the fix (the signal)

These changes are direct, EXTRACTED consequences of editing `polygons/polygons.py`:

| Change | Graph evidence | What it proves |
|---|---|---|
| **Polygon is no longer dead** | New edge `calc_polygon_details -calls-> polygons_polygons_polygon` | The god node that signal 1 flagged (degree 4, but with **no inbound usage** pre-fix) is now actually instantiated. The bug — a class defined but never used — is closed. |
| **`Object` -> `object`** | `polygons_polygons_polygon -inherits-> object` (pre-fix it inherited the undefined `Object`) | The `NameError` base-class typo is resolved; the inheritance edge now points at a real builtin. |
| **3 rationale nodes removed** | `polygons_polygons_rationale_{18,33,50}` gone | These nodes were extracted from the three `TODO:` comments at L18/L33/L50 (signal 5 — "uncertainty markers"). The fix *resolved* each TODO (general angle formula, single source of truth, generalized `draw_polygon`), so the uncertainty markers — and their nodes — disappear. **This is the clearest structural proof the documented weaknesses were addressed.** |

The edge count drop (20 -> 17) is dominated by the loss of the three `rationale_for` edges
that tethered those rationale nodes to the polygons subgraph. That is the *intended* effect:
fewer "I'm not sure how to do this" markers in the code.

### 2. Document-graph churn — an extraction artifact, NOT the fix

The removed `license_mit_license` / `mathsquiz_mathsquiz_final_py` nodes and the added
`readme` / `mathsquiz_readme_*` section nodes come from how this `graphify update` run
parsed the **Markdown/license documents** (it split READMEs into section nodes and dropped
the phantom `mathsquiz-final.py` reference that pointed at a file absent on disk — signal 3).
None of these nodes touch `polygons/polygons.py`. They are noted here for completeness and
explicitly excluded from the root-cause claim. The disappearance of the phantom
`mathsquiz-final.py` node is a mild bonus: re-extraction does not re-hallucinate a node for
a file that does not exist.

## Bottom line

The single PRE-FIX → POST-FIX code-graph change that matters: **the `Polygon` god node
gained an inbound `calls` edge and a valid `object` base.** Before the fix the graph showed
a well-connected class that nothing used (the "half-finished abstraction" smell); after the
fix the graph shows that class wired into the program's actual control flow. See
[`reports/root_cause.md`](root_cause.md) and [`reports/oop_improvement.md`](oop_improvement.md).
