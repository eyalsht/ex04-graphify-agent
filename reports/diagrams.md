# Architecture & Workflow Diagrams (R5.4.2 / R7.3)

> Rendered as Mermaid (GitHub renders these inline — no binary toolchain, fully
> reproducible from this file). The two agent-workflow diagrams use the **actual compiled
> node names** from `agent_workflow/graph_def.py`, so they are verified against the real
> `build_graph` topology (PHASE7-005 — see "Topology verification" at the end).

## 1. C4 — System Context (R5.4.2)

```mermaid
C4Context
    title System Context - EX04 Graphify + Obsidian Reverse-Engineering Agent
    Person(dev, "Developer", "Eyal / Imree - runs the CLI, reviews the fix + token report")
    System(ex04, "EX04 System", "Graph-guided debugging agent (src/ex04_graphify_agent/)")
    System_Ext(graph, "Graphify Graph", "artifacts/graphify/graph.json (PRE-FIX, read-only) + GRAPH_REPORT.md")
    System_Ext(vault, "Obsidian Vault", "obsidian/ - index.md + per-node *.md (PRE-FIX); hot.md generated by EX04")
    System_Ext(repo, "Target Repo (vendored)", "data/broken-python/ - polygons/polygons.py is the bug under fix")
    System_Ext(api, "LLM Provider API", "config-driven (Gemini; config/agent.json) - reached ONLY via the gatekeeper")
    System_Ext(reports, "Output Reports", "reports/ - token_comparison.md, diff, graph_diff, narratives")
    Rel(dev, ex04, "runs CLI, reads outputs")
    Rel(ex04, graph, "reads nodes/edges + confidence")
    Rel(ex04, vault, "reads index.md/hot.md; writes hot.md")
    Rel(ex04, repo, "reads + (POST-FIX) writes polygons.py")
    Rel(ex04, api, "LLM calls via gatekeeper")
    Rel(ex04, reports, "writes token + diff reports")
    Rel(dev, vault, "opens in Obsidian (screenshots, R5.4.1)")
```

## 2. C4 — Container (module) diagram

```mermaid
flowchart TB
    dev([Developer])
    subgraph EX04["EX04 System - src/ex04_graphify_agent/"]
        cli["cli.py<br/>(Typer, zero logic)"]
        sdk["sdk.py<br/>(facade - all business logic entry)"]
        gr["graph_reader/<br/>(parse graph, centrality, confidence filter)"]
        wd["weakness_detector/<br/>(6 PART-C signals -> hypotheses)"]
        ow["obsidian_writer/<br/>(index.md / hot.md / notes)"]
        aw["agent_workflow/<br/>(LangGraph: state + nodes)"]
        gk["gatekeeper/<br/>(rate-limit / retry / log / token counters)"]
        tc["token_comparison/<br/>(graph-guided vs naive -> report)"]
    end
    graphjson[("artifacts/graphify/graph.json<br/>(PRE-FIX, read-only)")]
    obs[("obsidian/*.md<br/>(index.md RO, hot.md written)")]
    src[("data/broken-python/polygons/polygons.py")]
    rep[("reports/*.md")]
    postgraph[("artifacts/graphify_post_fix/<br/>(POST-FIX graph)")]
    api([LLM Provider API<br/>config-driven, Gemini])
    dev --> cli --> sdk
    sdk --> gr & wd & ow & aw & tc
    gr --> graphjson
    ow --> graphjson
    ow --> obs
    wd --> gr
    aw --> gr & wd & obs & src & gk
    gk --> api
    tc --> aw & gk & rep
    aw -. POST-FIX writes .-> src
    aw -. POST-FIX graph .-> postgraph
```

## 3. Agent workflow — graph-guided **three-agent crew** (`run_type = graph_guided`)

The route is an orchestrator over three specialist subgraphs (ADR-0006), wired in
`_wire_graph_guided` (`agent_workflow/graph_def.py`) from the agent builders in
`agent_workflow/agents.py`. The **Navigator** agent owns `plan → read_vault`; the **Analyst**
agent owns `hypothesize → validate` with the **conditional** bounded validate→hypothesize loop;
the **Fixer** agent owns `fix`. A deterministic post-Analyst gate runs the Fixer only when a
finding validated, else reports; the orchestrator owns `report`.

```mermaid
stateDiagram-v2
    [*] --> navigator
    state "Navigator agent (plan, read_vault)" as navigator {
        [*] --> plan
        plan --> read_vault: read index.md / hot.md (NOT raw dump)
        read_vault --> [*]
    }
    state "Analyst agent (hypothesize, validate)" as analyst {
        [*] --> hypothesize
        hypothesize --> validate: weakness_detector -> 6-signal hypothesis
        validate --> hypothesize: source contradicts AND budget left (findings_tried < max)
        validate --> [*]: confirmed, or budget exhausted (max_findings_tried)
    }
    state "Fixer agent (fix)" as fixer {
        [*] --> fix
        fix --> [*]: write POST-FIX polygons.py + diff
    }
    navigator --> analyst
    analyst --> fixer: a finding validated
    analyst --> report: nothing validated (skip Fixer)
    fixer --> report
    report --> [*]
    note right of navigator
        Sources: artifacts/graphify/graph.json
        (via graph_reader), obsidian/index.md,
        obsidian/hot.md
    end note
    note right of analyst
        Inference discipline: graph proposes,
        source_file confirms. The fix target is
        resolved dynamically from
        current_hypothesis.source_file.
    end note
```

## 4. Agent workflow — naive baseline route (`run_type = naive`)

From `_wire_naive`: `plan → dump_repo → fix → report`. No `read_vault`, no `hypothesize`, no
`validate` — the "Lost in the Middle" baseline (R1.4 / ADR-0004).

```mermaid
stateDiagram-v2
    [*] --> plan
    plan --> dump_repo: read ALL of data/broken-python/** into context
    dump_repo --> fix: LLM localizes + patches from the raw dump
    fix --> report
    report --> [*]
    note right of dump_repo
        No graph_reader, no weakness_detector,
        no Obsidian. Shared plan/fix/report nodes
        with the graph-guided route so the
        gatekeeper token instrumentation is identical.
    end note
```

## 5. End-to-end pipeline (R5.5.1) — repo → graph → vault → agent → fix

```mermaid
flowchart LR
    A["1. Source<br/>polygons.py (broken)"] --> B["2. Graph (PRE-FIX)<br/>graph.json 23n/20e"]
    B --> C["3. Vault<br/>index.md + hot.md"]
    C --> D["4. Agent (graph-guided)<br/>read_vault->hypothesize->validate"]
    D --> E["5. Fix<br/>polygons.py (POST-FIX)"]
    E --> F["6. Graph (POST-FIX)<br/>graphify_post_fix/"]
    E --> G["7. Reports<br/>diff, root_cause, token_comparison"]
    F --> G
    classDef ro fill:#eee,stroke:#999;
    class B,C ro;
```

Each stage's inspectable artifact is tabulated in [`pipeline.md`](pipeline.md) (R5.5.2).

## Topology verification (PHASE7-005)

The graph-guided and naive diagrams above were checked against the compiled graph by
listing `agent_workflow.graph_def.node_names(run_type)` and the wired edges:

- **graph_guided:** `plan, read_vault, hypothesize, validate, fix, report` — the crew's nodes in
  execution order (derived from `agents.CREW`), composed as the Navigator → Analyst →
  (Fixer | report) subgraphs, with the bounded `validate → hypothesize` loop inside the Analyst
  and the `Analyst → {Fixer, report}` gate at the orchestrator (ADR-0006).
- **naive:** `plan, dump_repo, fix, report` (one flat monolithic agent — no crew).

Reproduce:

```bash
uv run python -c "from ex04_graphify_agent.agent_workflow.graph_def import node_names; \
print('graph_guided', node_names('graph_guided')); print('naive', node_names('naive'))"
```
